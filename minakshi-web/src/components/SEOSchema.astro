---
// This component can be used to add structured data to pages
interface Props {
    schema: object;
}

const { schema } = Astro.props;
---

<script type="application/ld+json" set:html={JSON.stringify(schema)} />
